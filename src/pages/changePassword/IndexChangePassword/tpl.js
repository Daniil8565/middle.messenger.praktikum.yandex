const tpl = `
        {{{avatar}}}
        <form class="form">
        {{{formSection}}}
        {{{formSection2}}}
        {{{formSection3}}}
        {{{button}}}
        </form>
`;

export default tpl;
