const tpl = `
<div>
        {{{avatar}}}
        <form class="form">
        {{{formSection}}}
        {{{formSection2}}}
        {{{formSection3}}}
        {{{button}}}
        </form>
</div>
        `;

export default tpl;
